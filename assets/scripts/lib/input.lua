local input = {
  key = Input_Key,
  mouse_button = Input_MouseButton,
  gamepad_button = Input_GamepadButton,
  action = Input_Action,
  keyboard = Input_Keyboard,
  mouse = Input_Mouse,
  gamepad = Input_Gamepad,
};

return input;
